# Engineering Intelligence — Historical Model

**Milestone:** APZTCMS-021

## Periods

daily | weekly | monthly | quarterly | release | custom

## Immutability

`HistoricalSnapshot.immutable === true`. Persistence rejects updates (`Historical snapshots are immutable`).

## Contents

Quality score, engineering health score, indicators, metrics bag, source refs (IDs only — no raw entity duplication).

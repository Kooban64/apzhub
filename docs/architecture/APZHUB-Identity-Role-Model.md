# Identity Role Model

`IdentityRole` is a role catalogue (metadata). Permission keys attach through `IdentityPermissionAssignment` (optional `roleId`). Runtime authorization evaluation remains Platform Authorization — not this package.

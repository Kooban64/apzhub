# Health, Diagnostics, Metrics

Diagnostics include rule counts, active/suppressed/ack counts, evaluation timings, event publication failures, counters for fired/ack/resolved/suppressed.

Health never reports READY when required dependency is unknown/unavailable. Disabled evaluation reports status `disabled`.

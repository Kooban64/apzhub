# Authorisation

Permissions: notifications.read|manage|preferences|send|retry|diagnostics|health|providers|templates|policies (+ legacy notification.*). Deny-by-default. Tenant/org/recipient ownership enforced.

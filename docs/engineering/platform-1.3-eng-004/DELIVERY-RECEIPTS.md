# Delivery Receipts

Receipt levels: requested → accepted_by_adapter → delivered (in-app durable) | failed | expired | unknown. In-app persistence counts as channel delivered; read state separate. No open tracking.

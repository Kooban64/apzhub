# Operations

Worker start/stop via service + `APZHUB_NOTIFICATION_WORKER_ENABLED`. Monitor diagnostics/queue. Retry/replay authorised. Rollback = disable flags. Incident: fail closed, preserve queue. POPIA review before prod. Smoke: enable flags → createIntent → inbox → mark read.

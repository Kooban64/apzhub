# Administration Navigation Metadata UI Guide

**Milestone:** APZADMIN-004

## Surfaces

- **Navigation** — navigation catalogue entries (label, visibility, permission keys, `routePath`)
- **Shortcuts** — shortcut catalogue linked to action ids

## Behaviour

- Metadata display and optional navigation to a declared `routePath`
- Does not mutate shell registration at runtime
- Does not execute shortcut actions

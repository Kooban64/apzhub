# Configuration Validation UI Guide

- Lists declarative validation rule metadata
- Invokes metadata-only validation via typed client
- Groups results as pass/fail messaging in UI
- Does not execute custom validators, scripts, or remote URLs
- Does not invent client-side validation rules

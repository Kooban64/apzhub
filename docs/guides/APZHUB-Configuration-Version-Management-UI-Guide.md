# Configuration Version Management UI Guide

- Lists versions for the selected configuration
- Shows immutable / current flags
- Displays `IMMUTABLE PUBLISHED VERSION` for immutable rows
- Supports publish-version when authorised
- Does not edit published versions
- Does not execute rollback
- Version comparison is deferred (insufficient safe API payload)

# APZHUB Metrics Formula Metadata HTTP Guide

**Milestone:** APZMETRICS-003

`/api/v1/metrics/formulas` manages formula **metadata** only (`expression`, `language`, lifecycle).

The HTTP API must not evaluate, execute, calculate, or resolve formulas. Execution belongs to future engines.

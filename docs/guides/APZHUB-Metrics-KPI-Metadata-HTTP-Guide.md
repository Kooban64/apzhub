# APZHUB Metrics KPI Metadata HTTP Guide

**Milestone:** APZMETRICS-003

`/api/v1/metrics/kpis`, `/kpi-groups`, and `/kpi-targets` manage KPI **definitions** only.

Do not evaluate KPIs, calculate values, or trigger KPI workflows via this API.

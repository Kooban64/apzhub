# Notification Recipient Privacy Guide (APZNOTIFY-003)

Responses expose only canonical contract fields. Do not expose private emails/phones without permission, provider account IDs, delivery/device/push tokens, Teams/Slack credentials, or webhook secrets. `addressHint` is optional metadata — not a credential store.

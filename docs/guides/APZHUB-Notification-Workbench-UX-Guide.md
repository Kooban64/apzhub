# Notification Workbench UX Guide (APZNOTIFY-004)

States: loading (`notifications-loading`), empty (`notifications-empty`), error (`notifications-error`), forbidden (`notifications-forbidden`), not found (`notifications-not-found`). Keyboard-selectable tables, ARIA toolbar/status/alert, responsive grids. No browser persistence, offline mode, or local editing.

# Workflow Workbench — Audit Timeline Guide

**Milestone:** APZWORKFLOW-004

Chronological audit entries for the selected workflow via `listWorkflowAudit`. Permission `workflow.audit` on the sidebar section; server enforces authorization.

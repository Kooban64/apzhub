# Workflow Workbench — Dependency View Guide

**Milestone:** APZWORKFLOW-004

Read-only dependency panel derived from workflow metadata (`categoryId`, `folderId`, `templateId`, `currentVersionId`). Relationships come from client lists/detail — no graph database, no Meilisearch.

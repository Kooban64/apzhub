# Workflow Workbench — Navigation Guide

**Milestone:** APZWORKFLOW-004

Activity Bar workspace `workflows` (`order` 52, icon `git-branch`). Sidebar children: overview, workflows, versions, templates, categories, folders, validation, audit, diagnostics. Manifest auto-discovery under `packages/workbench-framework/manifests/platform-workflows*/`.

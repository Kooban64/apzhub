# APZ Analytics — Administrator Guide (v1.0)

Grant `analytics.view` for Decision Companion. Operator surfaces need `analytics.admin` (or dataset/report grants).

Flags: `APZHUB_ANALYTICS_ENABLED`, `METABASE_INTEGRATION_ENABLED`. Mask engine brand from end users.

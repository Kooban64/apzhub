# Command Model Report — APZQEP-ENG-110D

| Field    | Value                   |
| -------- | ----------------------- |
| Location | `application/commands/` |

Immutable command objects with `kind` discriminators. Structural validation via `assert*` helpers (`EvidenceApplicationValidationError`). Business invariants remain in Domain.

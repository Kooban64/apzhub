# Query Model Report — APZQEP-ENG-110D

| Field    | Value                  |
| -------- | ---------------------- |
| Location | `application/queries/` |

Technology-neutral read intents. `searchEvidence` filters in-memory list results (no search index). `downloadEvidence` uses StoragePort after Domain delivery checks. `checkEvidenceAccess` returns grant inventory with `evaluation: "deferred"`.

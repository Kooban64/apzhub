# Application Commands

Create and update commands validate permissions and tenant context. Create appends version one; updates atomically persist the changed requirement and append the next content version where transaction support is provided. A reason is mandatory for updates.

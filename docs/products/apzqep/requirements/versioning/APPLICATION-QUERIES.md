# Application Queries

The service lists metadata, reads a specific version or latest version, compares two versions, and verifies integrity. Detailed snapshots are returned only by explicit version read. All queries enforce tenant scope and their dedicated permission.

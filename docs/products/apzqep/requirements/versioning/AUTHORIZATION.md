# Authorization

History uses `qep.requirements.versions.history`; version reads use `.versions.view`; comparison uses `.versions.compare`; integrity verification uses `.versions.verify`. Platform operation authorization and service checks are both authoritative.

# Canonical Snapshot

Snapshots contain requirement content and context required to reproduce its state: identifiers, fields, semantic version, references, attributes, lifecycle status, tenant and source revision. Object keys, tags, and custom attributes are canonicalized before hashing. Historical versions are not search primary results.

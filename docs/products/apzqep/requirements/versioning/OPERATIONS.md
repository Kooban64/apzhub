# Operations

Run database migrations before application deployment. Run the idempotent backfill only with an explicitly configured database and preserve its output as operational evidence. Use the versioning audit script to confirm required artefacts and pending owner acceptance.

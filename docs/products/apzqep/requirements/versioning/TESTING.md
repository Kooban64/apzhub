# Testing

Domain tests cover numbering, canonical snapshots, comparison, and integrity. Application tests cover append behavior and no-content-change rejection. Transport, client, and UI tests cover required reasons, history display, version reads, comparisons, and authorization.

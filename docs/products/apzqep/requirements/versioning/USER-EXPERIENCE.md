# User Experience

Requirement details display the current content version and provide a Version History panel. Edit requires a reason and explains that saving creates immutable history. History supports read-only snapshot detail and structured comparison. No “versioning engine” navigation exists.

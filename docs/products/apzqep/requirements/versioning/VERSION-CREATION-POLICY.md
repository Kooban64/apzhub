# Version Creation Policy

Creation appends the initial content version. A content-changing update requires a non-empty change reason and appends the next version. Archive and every lifecycle transition do **not** create content versions. Updates without canonical content changes are rejected.

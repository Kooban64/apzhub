# Explorer — ENG-050C

List-first inventory with status/type/query filters, pagination (limit 50), columns per OES-ARCH-012 Part 2. Selection opens Inspector via deep link. Review queue is Explorer with `status=under_review`.

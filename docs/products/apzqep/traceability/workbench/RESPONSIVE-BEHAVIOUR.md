# Responsive Behaviour — APZQEP-ENG-030C

- Multi-pane Detail uses existing QEP Workbench / shell grid conventions (ARCH-006)
- Narrow viewports: stack / collapse via shared shell patterns (same approach as Relationships Workbench)
- Selected Trace Link id and safe form state preserved across navigation via URL + React state
- Matrix falls back to list presentation when grid density is unsuitable
- No fixed desktop-only business logic

# Test Suite Domain — APZQEP-140-A

## Aggregate

`SuiteAggregate` = `SuiteNode` + `SuiteHistoryEntry[]`.

## Owned concepts

- Suite lifecycle · ownership · structure · hierarchy · metadata · governance · relationships · permissions

## Kinds

`standard` · `shared` · `reusable` · `template` · `reference` (library support reserved)

## Structure

Folders · nested folders · parent–child suites · shared / reusable / template / reference kinds.

## Metadata

Name, description, owner, project, tenant, version, status, priority, category, tags, risk, business area, application, component, created/updated/published/archived, custom metadata extension points.

## Permissions (consume platform RBAC)

`qep.suites.read` · `qep.suites.create` · `qep.suites.update` · `qep.suites.lifecycle` · `qep.suites.admin`

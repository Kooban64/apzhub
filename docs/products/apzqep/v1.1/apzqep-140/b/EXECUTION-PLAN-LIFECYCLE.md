# Execution Plan Lifecycle

`draft` → `in_review` → `approved` → `ready` → `scheduled` → `handed_off` → `archived` / `retired`

Also: `cancelled` from active planning states. READY = planning requirements satisfied. SCHEDULED = execution window exists. HANDED_OFF = Cap C may create execution from stable handoff id. EXECUTING is Cap C — out of scope.

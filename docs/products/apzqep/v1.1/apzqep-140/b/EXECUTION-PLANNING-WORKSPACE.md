# Execution Planning Workspace

Routes: `/workspace/qep/execution-plans`, `/new`, `/{id}`. Views: list, board (status columns), detail (lifecycle, schedule, readiness, assignments, environments, activity). Reference interaction pattern: Cap A Suite Workspace.

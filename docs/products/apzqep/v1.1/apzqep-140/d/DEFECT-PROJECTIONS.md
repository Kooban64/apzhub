# Defect Projections

QKI entity kind: `defect`. Definition: `DEFECT_PROJECTION_DEFINITION` (`qep.knowledge.defect.v1`).

Builder: `packages/qep-knowledge-index/src/projection/defect-builder.ts`.

Indexed: title, severity, priority, status, assignee, session/suite/evidence metadata.

Search consumes QKI only. Registered projections: **5** (evidence, suite, run, execution, defect).

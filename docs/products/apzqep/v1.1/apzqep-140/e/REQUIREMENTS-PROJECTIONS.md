# Requirements Projections

QKI entity kind: `requirement`. Definition: `REQUIREMENT_PROJECTION_DEFINITION` (`qep.knowledge.requirement.v1`).

Builder: `packages/qep-knowledge-index/src/projection/requirement-builder.ts`.

Indexed: title, status, priority, risk, suiteIds, overallCoverage, verificationStatus, uncovered, highRiskGap.

Registered projections: **6** (evidence, suite, run, execution, defect, requirement).

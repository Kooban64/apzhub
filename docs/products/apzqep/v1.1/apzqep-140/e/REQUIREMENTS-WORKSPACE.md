# Requirements Workspace

Routes:

- `/workspace/qep/enterprise-requirements` — list/filters
- `/new` — create
- `/matrix` — derived matrix
- `/coverage` — coverage dashboard
- `/{id}` — detail (coverage, graph, history, link suite)

Module: `modules/qep-enterprise-requirements/module.yaml`.

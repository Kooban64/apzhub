# Traceability Matrix

Workspace: `/workspace/qep/enterprise-requirements/matrix`

API: `GET /api/v1/qep/enterprise-requirements/matrix`

Each row: requirement + suite/plan/session/evidence/defect counts + derived coverage + verification status.

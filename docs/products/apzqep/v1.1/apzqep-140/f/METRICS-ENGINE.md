# Metrics Engine

`calculateMetrics(facts)` is pure. Metrics are never manually edited. Keys include requirement coverage, execution rates, open/critical defects, evidence integrity, planning readiness, etc.

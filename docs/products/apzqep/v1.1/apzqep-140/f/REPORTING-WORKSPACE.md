# Reporting Workspace

Routes: `/workspace/qep/enterprise-reporting`, `/dashboards`, `/dashboards/{id}`, `/templates`, `/reports`, `/metrics`.

Module: `modules/qep-enterprise-reporting/module.yaml`.

# VISUALIZATION-ENGINE — APZQEP-164

Descriptor builders (`buildKpi`, `buildChart`, `buildGauge`, `buildEvidenceViewer`, …) + `VisualizationRegistry` catalogue. Consumers render descriptors in product UI.

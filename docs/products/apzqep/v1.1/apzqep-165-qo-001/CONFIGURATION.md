# CONFIGURATION — QO-001

`OrchestrationKernelConfig`:

- `orchestrationId` (optional; default `orch_default`)
- `name` (default `platform-orchestration`)
- `enableDiagnostics` (default true)
- `metadata` (string map)

Validated at bootstrap via `validateOrchestrationKernelConfig`.

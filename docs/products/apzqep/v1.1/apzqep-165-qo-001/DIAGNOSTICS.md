# DIAGNOSTICS — QO-001

- `health()` — healthy/degraded/unhealthy + ready flag
- `readiness()` — ready boolean + state
- `version()` — version/programme/slice
- `diagnostics()` — counts + configValid

No HTTP routes in this slice — callable kernel APIs for future Gateway wiring (QO-016).

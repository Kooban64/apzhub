# RISK-MODEL — QO-005

Advisory risk only — never selects or triggers execution.

## Levels

`low` · `medium` · `high` · `critical`

## Inputs

Change magnitude, confidence near the change, known regressions, critical asset types (requirement/service/release), depth attenuation.

Peak node risk plus magnitude determines graph-level advisory risk.

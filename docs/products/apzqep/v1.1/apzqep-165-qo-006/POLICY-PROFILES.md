# POLICY-PROFILES — QO-006

Supported profile ids:

`developer_commit`, `feature_branch`, `pull_request`, `nightly`, `regression`, `release_candidate`, `production_release`, `emergency_fix`, `compliance_audit`, `custom`.

Profiles map to policies and declare a confidence target plus whether human approval is typically required (flag only — no approval workflow in this slice).

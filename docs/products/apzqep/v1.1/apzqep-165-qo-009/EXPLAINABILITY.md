# Explainability

Every Decision Package explains:

- why the conclusion was reached
- which inputs were consumed
- confidence rationale
- residual risk
- outstanding items
- decision profile (id + version)
- references to all upstream artefacts (flow, impact, policy, governance, approval)

Explainability is produced from the immutable package — never by re-running upstream engines.

# Search — APZQEP-ENG-040C

## Modes

- Platform Search (`executeSearchQuery`) for `verification_record`
- Advanced REST filters as fallback (status, outcome, subject, authority, priority keywords)
- Saved searches (local preference references)

Deep links use `/workspace/qep/verification/:id`.

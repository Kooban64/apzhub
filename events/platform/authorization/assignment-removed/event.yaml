manifestSchemaVersion: "1.0"
id: platform-authorization-assignment-removed
name: Platform Authorization Assignment Removed
version: 0.1.0
kind: event

metadata:
  category: platform
  description: Emitted when a role assignment is removed.

event:
  category: platform
  publisher: platform-authorization
  permission: platform.authorization.manage
  payload:
    assignmentId: string
    userId: string
    roleId: string

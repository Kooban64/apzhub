manifestSchemaVersion: "1.0"
id: platform-iam-member-suspended
name: Identity Lifecycle
version: 0.1.0
kind: event

metadata:
  category: platform
  description: IAM commercial programme event (platform-iam-member-suspended).

event:
  category: platform
  publisher: identity-lifecycle-service
  permission: identity.manage
  payload:
    organisationId: string
    userId: string
    actorId: string

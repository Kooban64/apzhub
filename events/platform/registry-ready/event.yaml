manifestSchemaVersion: "1.0"
id: platform-registry-ready
name: Platform Registry Ready
version: 0.1.0
kind: event

metadata:
  category: platform
  description: Emitted when the platform registry reaches ready state.

event:
  category: platform
  publisher: platform-registry
  payload:
    status: string
    capabilityCount: string

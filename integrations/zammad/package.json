{
  "name": "@apzhub/integration-zammad",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "Zammad CE integration adapter — operations, diagnostics & certification (OSS-102-07)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts integrations/zammad"
  },
  "dependencies": {
    "@apzhub/integration-sdk": "workspace:*",
    "@apzhub/platform-service-contracts": "workspace:*"
  }
}

/** HttpOnly session cookie (Phase 2 mock until real IdP). */
export const SESSION_COOKIE_NAME = "apzhub_session";

/** Short-lived OIDC CSRF state (Step 13). */
export const OIDC_STATE_COOKIE_NAME = "apzhub_oidc_state";

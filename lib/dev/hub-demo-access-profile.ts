/**
 * Shared bundle ids for hub demo / apzor testing seeds (local/staging + integration tests).
 * Per-service max overrides are defined in [`lib/dev/catalog-max-access.ts`].
 */
export const HUB_DEMO_BUNDLE_IDS = ["b-admin", "b-core"] as const;

manifestSchemaVersion: "1.0"
id: qep-administration
name: Administration
version: 0.1.0
kind: module

metadata:
  category: quality-engineering
  description: "Administration (M20) — SPR-APZQEP-210 MVP entry to org RBAC and platform consoles."
  product: apzqep
  moduleCatalogueId: M20
  slug: administration

dependencies:
  platform: []
  services:
    - qep-platform-service

module:
  category: quality-engineering
  status: active

workbench:
  navigation:
    level: sidebar
    workspace: qep
    parent: qep
    label: Administration
    icon: flask-conical
    route: /workspace/qep/administration
    order: 200
    permission: qep.administration.read
  view:
    title: Administration
    workspace: qep
    route: /workspace/qep/administration
    permission: qep.administration.read

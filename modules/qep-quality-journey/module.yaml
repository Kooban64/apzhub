manifestSchemaVersion: "1.0"
id: qep-quality-journey
name: Quality Journey
version: 0.1.0
kind: module

metadata:
  category: quality-engineering
  description: >
    Flagship F8 Change Quality Journey — one guided path for a change:
    Impact → Design → Evidence → RC → human GO/NO-GO. Read-only glue; never auto-certifies.
  product: apzqep
  slug: quality-journey
  programme: FLAGSHIP-F8

dependencies:
  platform: []
  services:
    - qep-platform-service

module:
  category: quality-engineering
  status: active

workbench:
  navigation:
    level: sidebar
    workspace: qep
    parent: qep
    label: Quality Journey
    icon: route
    route: /workspace/qep/quality-journey
    order: 68
    permission: qep.scm.read
  view:
    title: Change Quality Journey
    workspace: qep
    route: /workspace/qep/quality-journey
    permission: qep.scm.read

/** Platform Activity & Timeline catalogue semver — stamped on bootstrap descriptors. */
export const ACTIVITY_TIMELINE_PLATFORM_VERSION = "1.0.0" as const;

export type ActivityTimelinePlatformVersion = typeof ACTIVITY_TIMELINE_PLATFORM_VERSION;

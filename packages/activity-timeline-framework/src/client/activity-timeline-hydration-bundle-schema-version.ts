/** Combined client hydration bundle schema version — metadata registries only (AT-009). */
export const ACTIVITY_TIMELINE_HYDRATION_BUNDLE_SCHEMA_VERSION = 1 as const;

export type ActivityTimelineHydrationBundleSchemaVersion =
  typeof ACTIVITY_TIMELINE_HYDRATION_BUNDLE_SCHEMA_VERSION;

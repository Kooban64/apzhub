/** Platform release version stamped on built-in catalogue actions (AF-009). */
export const ACTION_FRAMEWORK_PLATFORM_VERSION = "0.3.0" as const;

export type ActionFrameworkPlatformVersion = typeof ACTION_FRAMEWORK_PLATFORM_VERSION;

{
  "name": "@apzhub/configuration-core",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Configuration core — lifecycle, hierarchy, validation metadata (APZCONFIG-001)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/configuration-core"
  },
  "dependencies": {
    "@apzhub/configuration-contracts": "workspace:*"
  }
}

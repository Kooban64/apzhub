{
  "name": "@apzhub/document-core",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Document core — storage contracts, integrity, coordinator, domain (APZDOCS-001/003)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "vitest run --config ../../vitest.config.ts packages/document-core"
  },
  "dependencies": {
    "@apzhub/document-contracts": "workspace:*"
  }
}

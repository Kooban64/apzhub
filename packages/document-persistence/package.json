{
  "name": "@apzhub/document-persistence",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Document persistence — PostgreSQL + in-memory metadata (APZDOCS-002)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "vitest run --config ../../vitest.config.ts packages/document-persistence"
  },
  "dependencies": {
    "@apzhub/config": "workspace:*",
    "@apzhub/document-contracts": "workspace:*",
    "@apzhub/document-core": "workspace:*",
    "drizzle-orm": "^0.45.2"
  }
}

# @apzhub/document-storage

Production DocumentStorageProvider implementations (APZDOCS-002):

- Memory (tests)
- Filesystem (local/dev/on-prem with explicit flag)
- S3-compatible (AWS S3 / MinIO)

Azure Blob and GCS remain unimplemented placeholders in core contracts.

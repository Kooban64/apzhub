{
  "name": "@apzhub/identity-contracts",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Identity Administration contracts — models, permissions, gateway (APZIDENTITY-002)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/identity-contracts"
  },
  "dependencies": {}
}

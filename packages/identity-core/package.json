{
  "name": "@apzhub/identity-core",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Identity Administration domain core (APZIDENTITY-002)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/identity-core"
  },
  "dependencies": {
    "@apzhub/identity-contracts": "workspace:*"
  }
}

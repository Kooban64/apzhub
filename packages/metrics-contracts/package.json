{
  "name": "@apzhub/metrics-contracts",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Metrics contracts — models, permissions, gateway facets (APZMETRICS-002)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/metrics-contracts"
  },
  "dependencies": {}
}

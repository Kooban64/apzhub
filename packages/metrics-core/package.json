{
  "name": "@apzhub/metrics-core",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Metrics domain core — validation, lifecycle, platform service (APZMETRICS-002)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/metrics-core"
  },
  "dependencies": {
    "@apzhub/metrics-contracts": "workspace:*"
  }
}

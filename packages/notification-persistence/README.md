# `@apzhub/notification-persistence`

Platform Notification persistence — PostgreSQL + in-memory metadata (APZNOTIFY-001). Version **0.1.0**.

Migrations: **0046** / **0047** (RLS). Production requires PostgreSQL — no silent in-memory fallback.

{
  "name": "@apzhub/observe-core",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Observability domain core — validation, lifecycle, repository ports (APZOBSERVE-001)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/observe-core"
  },
  "dependencies": {
    "@apzhub/observe-contracts": "workspace:*"
  }
}

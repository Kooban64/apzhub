{
  "name": "@apzhub/platform-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "QEP-elevated automation engine (APZQEP-161). Not an APS-002 Platform Service (fails Two-Consumer Rule). See APS-E-05.",
  "exports": {
    ".": "./src/index.ts",
    "./contracts": "./src/contracts/index.ts",
    "./providers/playwright": "./src/providers/playwright/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/platform-automation"
  },
  "peerDependencies": {
    "playwright": "^1.40.0"
  },
  "peerDependenciesMeta": {
    "playwright": {
      "optional": true
    }
  }
}

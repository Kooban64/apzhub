/**
 * F3 deepen — placeholders removed; all matrix providers are active ingest/live.
 * Re-exports kept for enhance-only import compatibility.
 */

export { createPlaceholderProviders, PLACEHOLDER_IDS } from "./matrix-providers";

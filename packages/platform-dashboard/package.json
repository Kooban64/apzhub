{
  "name": "@apzhub/platform-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "QEP-elevated dashboard engine (APZQEP-164). Not an APS-002 Platform Service until Two-Consumer Rule met. See APS-E-05.",
  "exports": {
    ".": "./src/index.ts",
    "./contracts": "./src/contracts/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/platform-dashboard"
  }
}

{
  "name": "@apzhub/platform-orchestration",
  "version": "0.1.16",
  "private": true,
  "type": "module",
  "description": "QEP-elevated quality orchestration (APZQEP-165). Not an APS-002 Platform Service (fails Two-Consumer Rule). See APS-E-05.",
  "exports": {
    ".": "./src/index.ts",
    "./contracts": "./src/contracts/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/platform-orchestration"
  },
  "dependencies": {
    "@apzhub/config": "workspace:*",
    "drizzle-orm": "^0.45.2"
  }
}

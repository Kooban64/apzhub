{
  "name": "@apzhub/platform-processing",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Processing — reliable event execution engine (APZQEP-120-S09). Knows how to execute, never why.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/platform-processing"
  },
  "dependencies": {
    "@apzhub/platform-outbox": "workspace:*"
  }
}

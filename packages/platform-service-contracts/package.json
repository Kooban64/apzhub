{
  "name": "@apzhub/platform-service-contracts",
  "version": "0.18.0",
  "private": true,
  "type": "module",
  "description": "Vendor-neutral platform service interfaces, DTOs, and query contracts (APZWORKFLOW-002 stub)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/platform-service-contracts"
  },
  "dependencies": {
    "@apzhub/testing-contracts": "workspace:*"
  }
}

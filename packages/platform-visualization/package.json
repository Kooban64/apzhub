{
  "name": "@apzhub/platform-visualization",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "APZHUB reusable Visualization Platform — charts, timelines, heatmaps, evidence viewers (APZQEP-164). Presentation-only; no business calculations.",
  "exports": {
    ".": "./src/index.ts",
    "./contracts": "./src/contracts/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/platform-visualization"
  }
}

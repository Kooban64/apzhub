/**
 * Audit adapter scaffold — non-functional (APZQEP-ENG-110A).
 */
export type AuditAdapterScaffoldId = "EvidenceAuditAdapter";

export interface AuditAdapterScaffold {
  readonly adapterId: AuditAdapterScaffoldId;
}

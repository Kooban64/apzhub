{
  "name": "@apzhub/qep-reporting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "APZQEP Enterprise Reporting & Analytics — Capability F (APZQEP-140-F). Derived read model over Caps A–E and QKI. Never a business SoR.",
  "exports": {
    ".": "./src/index.ts",
    "./domain": "./src/domain/index.ts",
    "./application": "./src/application/index.ts",
    "./presentation": "./src/presentation/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/qep-reporting"
  },
  "dependencies": {
    "@apzhub/platform-processing": "workspace:*",
    "@apzhub/qep-command": "workspace:*",
    "@apzhub/qep-knowledge-index": "workspace:*",
    "@apzhub/qep-notification": "workspace:*"
  }
}

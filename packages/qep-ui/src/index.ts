import { QEP_PRODUCT_NAME } from "@apzhub/qep-types";

export const QEP_UI_VERSION = "0.1.0";

/** Placeholder label helper — presentation tokens arrive with component SDK work later. */
export function getQepProductLabel(): string {
  return QEP_PRODUCT_NAME;
}

{
  "name": "@apzhub/reporting-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Reporting engine — template binding, renderers, output providers (APZREPORT-001)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "vitest run --config ../../vitest.config.ts packages/reporting-core"
  },
  "dependencies": {
    "@apzhub/reporting-contracts": "workspace:*"
  }
}

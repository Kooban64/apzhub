{
  "name": "@apzhub/search-projects",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Projects Search Publication Adapter — canonical Projects → Search Integration Framework (APZSEARCH-010)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/search-projects"
  },
  "dependencies": {
    "@apzhub/platform-service-contracts": "workspace:*",
    "@apzhub/search-contracts": "workspace:*",
    "@apzhub/search-integration": "workspace:*"
  }
}

{
  "name": "@apzhub/search-time",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "APZHUB Time Search Publication Adapter — canonical Time → Search Integration Framework (R12-SEARCH-01)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/search-time"
  },
  "dependencies": {
    "@apzhub/platform-service-contracts": "workspace:*",
    "@apzhub/search-contracts": "workspace:*",
    "@apzhub/search-integration": "workspace:*"
  }
}

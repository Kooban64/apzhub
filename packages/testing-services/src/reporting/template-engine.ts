/** @deprecated Import from `@apzhub/reporting-core` — re-export for TCMS compatibility (APZREPORT-001). */
export {
  bindTemplateToDocument,
  validateTemplateBinding,
  type BindTemplateArgs,
} from "@apzhub/reporting-core";

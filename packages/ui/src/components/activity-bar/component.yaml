manifestSchemaVersion: "1.0"
id: activity-bar
name: Activity Bar
version: 0.1.0
kind: component

metadata:
  category: shell
  description: Primary navigation activity bar for the APZHUB desktop shell.

dependencies:
  platform: []

health:
  enabled: false

documentation:
  storybook: packages/ui/src/components/shell-layout.stories.tsx

tests:
  unit: true
  accessibility: true

component:
  theme:
    supportsDarkMode: true

manifestSchemaVersion: "1.0"
id: platform-admin-actions
name: Actions
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Administration — Actions sidebar (APZADMIN-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: administration
    parent: platform-admin
    label: Actions
    icon: zap
    route: /workspace/administration/actions
    order: 70
    permission: admin.read
  view:
    title: Actions
    workspace: administration
    route: /workspace/administration/actions
    permission: admin.read

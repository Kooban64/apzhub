manifestSchemaVersion: "1.0"
id: platform-administration
name: Platform Operations
version: 0.2.0
kind: module

metadata:
  category: platform
  description: Platform Operations workspace — central operational interface (M8-03).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: activity-bar
    workspace: operations
    icon: shield
    route: /workspace/operations
    order: 20
    permission: platform.nav.administration.view
  view:
    title: Platform Operations
    workspace: operations
    route: /workspace/operations
    default: true
    permission: platform.nav.administration.view

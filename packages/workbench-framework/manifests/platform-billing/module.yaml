manifestSchemaVersion: "1.0"
id: platform-billing
name: Billing
version: 0.1.0
kind: module

metadata:
  category: platform
  description: >
    Commercial billing workspace — catalogue, PayFast, invoices, statements,
    entitlements, and Cursor-like dunning notices (SPR-IAM-COMMERCIAL-001).
  programme: SPR-IAM-COMMERCIAL-001

dependencies:
  platform: []
  services:
    - billing-service

module:
  category: platform
  status: active

workbench:
  navigation:
    level: sidebar
    workspace: administration
    parent: platform-administration
    label: Billing
    icon: credit-card
    route: /workspace/billing
    order: 15
    permission: billing.read
  view:
    title: Billing
    workspace: administration
    route: /workspace/billing
    permission: billing.read

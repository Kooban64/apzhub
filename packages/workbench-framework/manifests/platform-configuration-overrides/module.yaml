manifestSchemaVersion: "1.0"
id: platform-configuration-overrides
name: Overrides
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Configuration — Overrides sidebar (APZCONFIG-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: configuration
    parent: platform-configuration
    label: Overrides
    icon: git-compare
    route: /workspace/configuration/overrides
    order: 60
    permission: configuration.manage
  view:
    title: Overrides
    workspace: configuration
    route: /workspace/configuration/overrides
    permission: configuration.manage

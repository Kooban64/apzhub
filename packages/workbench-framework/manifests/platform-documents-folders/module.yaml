manifestSchemaVersion: "1.0"
id: platform-documents-folders
name: Folders
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Documents — Folders sidebar (APZDOCS-005).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Folders
    icon: folder
    route: /workspace/documents/folders
    order: 50
    permission: document.folder.read
  view:
    title: Folders
    workspace: documents
    route: /workspace/documents/folders
    permission: document.folder.read

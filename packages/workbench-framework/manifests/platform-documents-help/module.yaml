manifestSchemaVersion: "1.0"
id: platform-documents-help
name: Help
version: 0.1.0
kind: module

metadata:
  category: platform
  description: APZ Documents — Help (work companion).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Help
    icon: circle-help
    route: /workspace/documents/help
    order: 120
    permission: document.read
  view:
    title: Help
    workspace: documents
    route: /workspace/documents/help
    permission: document.read

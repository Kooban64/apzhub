manifestSchemaVersion: "1.0"
id: platform-documents-library
name: Documents
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Documents — Documents sidebar (APZDOCS-005).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Documents
    icon: files
    route: /workspace/documents/documents
    order: 20
    permission: document.read
  view:
    title: Documents
    workspace: documents
    route: /workspace/documents/documents
    permission: document.read

manifestSchemaVersion: "1.0"
id: platform-documents-metadata
name: Metadata
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Documents — Metadata sidebar (APZDOCS-005).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Metadata
    icon: file-json
    route: /workspace/documents/metadata
    order: 110
    permission: document.metadata.read
  view:
    title: Metadata
    workspace: documents
    route: /workspace/documents/metadata
    permission: document.metadata.read

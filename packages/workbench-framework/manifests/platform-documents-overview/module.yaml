manifestSchemaVersion: "1.0"
id: platform-documents-overview
name: Overview
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Documents — Overview sidebar (APZDOCS-005).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Overview
    icon: layout-dashboard
    route: /workspace/documents/overview
    order: 10
    permission: document.read
  view:
    title: Overview
    workspace: documents
    route: /workspace/documents/overview
    permission: document.read

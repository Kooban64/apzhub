manifestSchemaVersion: "1.0"
id: platform-documents-relationships
name: Relationships
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Documents — Relationships sidebar (APZDOCS-005).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Relationships
    icon: link
    route: /workspace/documents/relationships
    order: 70
    permission: document.relationship.read
  view:
    title: Relationships
    workspace: documents
    route: /workspace/documents/relationships
    permission: document.relationship.read

manifestSchemaVersion: "1.0"
id: platform-documents-retention
name: Retention
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Documents — Retention sidebar (APZDOCS-005).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Retention
    icon: clock
    route: /workspace/documents/retention
    order: 80
    permission: document.retention
  view:
    title: Retention
    workspace: documents
    route: /workspace/documents/retention
    permission: document.retention

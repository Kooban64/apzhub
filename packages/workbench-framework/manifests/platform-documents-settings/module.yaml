manifestSchemaVersion: "1.0"
id: platform-documents-settings
name: Settings
version: 0.1.0
kind: module

metadata:
  category: platform
  description: APZ Documents — Settings (product preferences only).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Settings
    icon: settings
    route: /workspace/documents/settings
    order: 130
    permission: document.read
  view:
    title: Settings
    workspace: documents
    route: /workspace/documents/settings
    permission: document.read

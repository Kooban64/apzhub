manifestSchemaVersion: "1.0"
id: platform-documents-tags
name: Tags
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Documents — Tags sidebar (APZDOCS-005).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Tags
    icon: tags
    route: /workspace/documents/tags
    order: 60
    permission: document.tag.read
  view:
    title: Tags
    workspace: documents
    route: /workspace/documents/tags
    permission: document.tag.read

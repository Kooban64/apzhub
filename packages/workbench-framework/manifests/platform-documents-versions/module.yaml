manifestSchemaVersion: "1.0"
id: platform-documents-versions
name: Versions
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Documents — Versions sidebar (APZDOCS-005).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: documents
    parent: platform-documents
    label: Versions
    icon: git-branch
    route: /workspace/documents/versions
    order: 30
    permission: document.version.read
  view:
    title: Versions
    workspace: documents
    route: /workspace/documents/versions
    permission: document.version.read

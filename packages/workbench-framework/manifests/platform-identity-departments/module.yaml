manifestSchemaVersion: "1.0"
id: platform-identity-departments
name: Departments
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Identity — Departments sidebar (APZIDENTITY-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: identity
    parent: platform-identity
    label: Departments
    icon: network
    route: /workspace/identity/departments
    order: 70
    permission: identity.read
  view:
    title: Departments
    workspace: identity
    route: /workspace/identity/departments
    permission: identity.read

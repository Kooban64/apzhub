manifestSchemaVersion: "1.0"
id: platform-identity-memberships
name: Memberships
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Identity — Memberships sidebar (APZIDENTITY-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: identity
    parent: platform-identity
    label: Memberships
    icon: link
    route: /workspace/identity/memberships
    order: 90
    permission: identity.read
  view:
    title: Memberships
    workspace: identity
    route: /workspace/identity/memberships
    permission: identity.read

manifestSchemaVersion: "1.0"
id: platform-metrics-aggregations
name: Metric Aggregations
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Metrics — Metric Aggregations sidebar (APZMETRICS-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: metrics
    parent: platform-metrics
    label: Metric Aggregations
    icon: sigma
    route: /workspace/metrics/aggregations
    order: 110
    permission: metrics.definition
  view:
    title: Metric Aggregations
    workspace: metrics
    route: /workspace/metrics/aggregations
    permission: metrics.definition

manifestSchemaVersion: "1.0"
id: platform-metrics-overview
name: Overview
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Metrics — Overview sidebar (APZMETRICS-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: metrics
    parent: platform-metrics
    label: Overview
    icon: layout-dashboard
    route: /workspace/metrics/overview
    order: 10
    permission: metrics.read
  view:
    title: Overview
    workspace: metrics
    route: /workspace/metrics/overview
    permission: metrics.read

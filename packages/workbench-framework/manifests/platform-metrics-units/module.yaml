manifestSchemaVersion: "1.0"
id: platform-metrics-units
name: Metric Units
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Metrics — Metric Units sidebar (APZMETRICS-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: metrics
    parent: platform-metrics
    label: Metric Units
    icon: ruler
    route: /workspace/metrics/units
    order: 90
    permission: metrics.metadata
  view:
    title: Metric Units
    workspace: metrics
    route: /workspace/metrics/units
    permission: metrics.metadata

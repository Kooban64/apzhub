manifestSchemaVersion: "1.0"
id: platform-notifications-inbox
name: Notifications
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Notifications — Notifications sidebar (APZNOTIFY-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: notifications
    parent: platform-notifications
    label: Notifications
    icon: inbox
    route: /workspace/notifications/inbox
    order: 20
    permission: notification.read
  view:
    title: Notification Centre
    workspace: notifications
    route: /workspace/notifications/inbox
    permission: notification.read

manifestSchemaVersion: "1.0"
id: platform-notifications-overview
name: Overview
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Notifications — Overview sidebar (APZNOTIFY-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: notifications
    parent: platform-notifications
    label: Overview
    icon: layout-dashboard
    route: /workspace/notifications/overview
    order: 10
    permission: notification.read
  view:
    title: Overview
    workspace: notifications
    route: /workspace/notifications/overview
    permission: notification.read

manifestSchemaVersion: "1.0"
id: platform-notifications-preferences
name: Preferences
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Notifications — Preferences sidebar (APZNOTIFY-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: notifications
    parent: platform-notifications
    label: Preferences
    icon: sliders-horizontal
    route: /workspace/notifications/preferences
    order: 40
    permission: notification.preference
  view:
    title: Preferences
    workspace: notifications
    route: /workspace/notifications/preferences
    permission: notification.preference

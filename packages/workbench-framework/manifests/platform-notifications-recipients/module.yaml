manifestSchemaVersion: "1.0"
id: platform-notifications-recipients
name: Recipients
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Notifications — Recipients sidebar (APZNOTIFY-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: notifications
    parent: platform-notifications
    label: Recipients
    icon: users
    route: /workspace/notifications/recipients
    order: 70
    permission: notification.read
  view:
    title: Recipients
    workspace: notifications
    route: /workspace/notifications/recipients
    permission: notification.read

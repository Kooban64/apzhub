manifestSchemaVersion: "1.0"
id: platform-observability-metric-definitions
name: Metric Definitions
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Observability — Metric Definitions sidebar (Telemetry Metadata) (APZOBSERVE-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: observability
    parent: platform-observability
    label: Metric Definitions
    icon: gauge
    route: /workspace/observability/metric-definitions
    order: 90
    permission: observe.metrics
  view:
    title: Metric Definitions
    workspace: observability
    route: /workspace/observability/metric-definitions
    permission: observe.metrics

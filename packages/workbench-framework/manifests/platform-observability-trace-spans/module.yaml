manifestSchemaVersion: "1.0"
id: platform-observability-trace-spans
name: Trace Spans
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Observability — Trace Spans sidebar (Telemetry Metadata) (APZOBSERVE-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: observability
    parent: platform-observability
    label: Trace Spans
    icon: git-commit-horizontal
    route: /workspace/observability/trace-spans
    order: 130
    permission: observe.traces
  view:
    title: Trace Spans
    workspace: observability
    route: /workspace/observability/trace-spans
    permission: observe.traces

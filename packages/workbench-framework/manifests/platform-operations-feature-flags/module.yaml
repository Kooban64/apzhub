manifestSchemaVersion: "1.0"
id: platform-operations-feature-flags
name: Feature Flags
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Operations — Feature Flags (M8-03).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: operations
    parent: platform-administration
    label: Feature Flags
    icon: flag
    route: /workspace/operations/feature-flags
    order: 130
    permission: platform.nav.administration.view
  view:
    title: Feature Flags
    workspace: operations
    route: /workspace/operations/feature-flags
    permission: platform.nav.administration.view

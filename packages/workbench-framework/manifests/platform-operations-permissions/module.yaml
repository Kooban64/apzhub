manifestSchemaVersion: "1.0"
id: platform-operations-permissions
name: Permissions
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Operations — Permissions (M8-03).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: operations
    parent: platform-administration
    label: Permissions
    icon: key
    route: /workspace/operations/permissions
    order: 40
    permission: platform.nav.administration.view
  view:
    title: Permissions
    workspace: operations
    route: /workspace/operations/permissions
    permission: platform.nav.administration.view

manifestSchemaVersion: "1.0"
id: platform-operations-permissions
name: Permissions
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Operations — Permissions (M8-03).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: administration
    parent: platform-administration
    label: Permissions
    icon: key
    route: /workspace/administration/permissions
    order: 40
    permission: platform.nav.administration.view
  view:
    title: Permissions
    workspace: administration
    route: /workspace/administration/permissions
    permission: platform.nav.administration.view

manifestSchemaVersion: "1.0"
id: platform-operations-products
name: Products
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Operations — Products (M8-03).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: operations
    parent: platform-administration
    label: Products
    icon: package
    route: /workspace/operations/products
    order: 50
    permission: platform.nav.administration.view
  view:
    title: Products
    workspace: operations
    route: /workspace/operations/products
    permission: platform.nav.administration.view

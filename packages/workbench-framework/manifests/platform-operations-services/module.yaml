manifestSchemaVersion: "1.0"
id: platform-operations-services
name: Services
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Operations — Services (M8-03).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: operations
    parent: platform-administration
    label: Services
    icon: server
    route: /workspace/operations/services
    order: 60
    permission: platform.nav.administration.view
  view:
    title: Services
    workspace: operations
    route: /workspace/operations/services
    permission: platform.nav.administration.view

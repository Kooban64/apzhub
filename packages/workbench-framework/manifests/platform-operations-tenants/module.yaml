manifestSchemaVersion: "1.0"
id: platform-operations-tenants
name: Tenants
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Operations — Tenants (M8-03).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: operations
    parent: platform-administration
    label: Tenants
    icon: building
    route: /workspace/operations/tenants
    order: 10
    permission: platform.nav.administration.view
  view:
    title: Tenants
    workspace: operations
    route: /workspace/operations/tenants
    permission: platform.nav.administration.view

manifestSchemaVersion: "1.0"
id: platform-operations-users
name: Users
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Operations — Users (M8-03).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: operations
    parent: platform-administration
    label: Users
    icon: users
    route: /workspace/operations/users
    order: 20
    permission: platform.nav.administration.view
  view:
    title: Users
    workspace: operations
    route: /workspace/operations/users
    permission: platform.nav.administration.view

manifestSchemaVersion: "1.0"
id: platform-reporting-history
name: History
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Reporting — History sidebar.

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: reporting
    parent: platform-reporting
    label: History
    icon: history
    route: /workspace/reporting/history
    order: 30
    permission: report.view
  view:
    title: History
    workspace: reporting
    route: /workspace/reporting/history
    permission: report.view

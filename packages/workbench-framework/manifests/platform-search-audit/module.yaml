manifestSchemaVersion: "1.0"
id: platform-search-audit
name: Audit
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Search — Audit sidebar (APZSEARCH-007).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: search
    parent: platform-search
    label: Audit
    icon: scroll-text
    route: /workspace/search/audit
    order: 90
    permission: search.audit
  view:
    title: Audit
    workspace: search
    route: /workspace/search/audit
    permission: search.audit

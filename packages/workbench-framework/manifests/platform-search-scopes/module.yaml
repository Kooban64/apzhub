manifestSchemaVersion: "1.0"
id: platform-search-scopes
name: Scopes
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Search — Scopes sidebar (APZSEARCH-007).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: search
    parent: platform-search
    label: Scopes
    icon: layers
    route: /workspace/search/scopes
    order: 70
    permission: search.scope.list
  view:
    title: Scopes
    workspace: search
    route: /workspace/search/scopes
    permission: search.scope.list

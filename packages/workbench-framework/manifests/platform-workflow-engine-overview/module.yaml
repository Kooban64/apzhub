manifestSchemaVersion: "1.0"
id: platform-workflow-engine-overview
name: Overview
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Workflow Engine — Overview sidebar (APZWORKFLOW-009).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: workflow-engine
    parent: platform-workflow-engine
    label: Overview
    icon: layout-dashboard
    route: /workspace/workflow-engine/overview
    order: 10
    permission: workflow.engine.read
  view:
    title: Overview
    workspace: workflow-engine
    route: /workspace/workflow-engine/overview
    permission: workflow.engine.read

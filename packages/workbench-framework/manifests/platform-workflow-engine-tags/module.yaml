manifestSchemaVersion: "1.0"
id: platform-workflow-engine-tags
name: Tags
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Workflow Engine — Tags sidebar (APZWORKFLOW-009).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: workflow-engine
    parent: platform-workflow-engine
    label: Tags
    icon: tags
    route: /workspace/workflow-engine/tags
    order: 60
    permission: workflow.admin
  view:
    title: Tags
    workspace: workflow-engine
    route: /workspace/workflow-engine/tags
    permission: workflow.admin

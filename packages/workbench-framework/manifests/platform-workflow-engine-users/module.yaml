manifestSchemaVersion: "1.0"
id: platform-workflow-engine-users
name: Users
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Workflow Engine — Users sidebar (APZWORKFLOW-009).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: workflow-engine
    parent: platform-workflow-engine
    label: Users
    icon: users
    route: /workspace/workflow-engine/users
    order: 50
    permission: workflow.engine.read
  view:
    title: Users
    workspace: workflow-engine
    route: /workspace/workflow-engine/users
    permission: workflow.engine.read

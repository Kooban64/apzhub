manifestSchemaVersion: "1.0"
id: platform-workflow-engine-workflows
name: Workflows
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Workflow Engine — Workflows sidebar (APZWORKFLOW-009).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: workflow-engine
    parent: platform-workflow-engine
    label: Workflows
    icon: git-branch
    route: /workspace/workflow-engine/workflows
    order: 20
    permission: workflow.engine.read
  view:
    title: Workflows
    workspace: workflow-engine
    route: /workspace/workflow-engine/workflows
    permission: workflow.engine.read

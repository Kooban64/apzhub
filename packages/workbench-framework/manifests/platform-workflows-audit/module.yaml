manifestSchemaVersion: "1.0"
id: platform-workflows-audit
name: Audit
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Workflows — Audit sidebar (APZWORKFLOW-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: workflows
    parent: platform-workflows
    label: Audit
    icon: scroll-text
    route: /workspace/workflows/audit
    order: 80
    permission: workflow.audit
  view:
    title: Audit
    workspace: workflows
    route: /workspace/workflows/audit
    permission: workflow.audit

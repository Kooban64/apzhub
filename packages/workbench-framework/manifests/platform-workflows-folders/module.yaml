manifestSchemaVersion: "1.0"
id: platform-workflows-folders
name: Folders
version: 0.1.0
kind: module

metadata:
  category: platform
  description: Platform Workflows — Folders sidebar (APZWORKFLOW-004).

dependencies:
  platform: []

module:
  category: platform

workbench:
  navigation:
    level: sidebar
    workspace: workflows
    parent: platform-workflows
    label: Folders
    icon: folder
    route: /workspace/workflows/folders
    order: 60
    permission: workflow.view
  view:
    title: Folders
    workspace: workflows
    route: /workspace/workflows/folders
    permission: workflow.view

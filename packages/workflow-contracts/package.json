{
  "name": "@apzhub/workflow-contracts",
  "version": "0.4.2",
  "private": true,
  "type": "module",
  "description": "APZHUB Workflow Platform contracts — provider-neutral models, permissions, service interfaces (APZHUB-PLATFORM-WORKFLOW-003)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/workflow-contracts"
  },
  "dependencies": {}
}

# `@apzhub/workflow-core`

APZHUB Platform Workflow domain core (APZWORKFLOW-001).

Lifecycle transitions, structural/reference/parameter/version/lifecycle validation, and foundation composition. Depends only on `@apzhub/workflow-contracts`. No persistence, HTTP, or n8n.

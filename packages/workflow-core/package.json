{
  "name": "@apzhub/workflow-core",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "APZHUB Platform Workflow core — lifecycle, validation, domain service (APZWORKFLOW-002)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src/",
    "test": "cd ../.. && vitest run --config vitest.config.ts packages/workflow-core"
  },
  "dependencies": {
    "@apzhub/workflow-contracts": "workspace:*"
  }
}

manifestSchemaVersion: "1.0"
id: projects-dashboard
name: Operational Workspace
version: 0.1.0
kind: module

metadata:
  category: productivity
  description: APZ Projects — Operational Workspace (Release 3.0).

dependencies:
  platform: []
  services:
    - project-service

module:
  category: productivity
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: projects
    parent: projects
    label: Operational Workspace
    icon: layout-dashboard
    route: /workspace/projects
    order: 10
    permission: projects.view
  view:
    title: Operational Workspace
    workspace: projects
    route: /workspace/projects
    permission: projects.view

manifestSchemaVersion: "1.0"
id: projects-search
name: Search
version: 0.1.0
kind: module

metadata:
  category: productivity
  description: APZ Projects — Search.

dependencies:
  platform: []
  services:
    - project-service

module:
  category: productivity
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: projects
    parent: projects
    label: Search
    icon: search
    route: /workspace/projects/search
    order: 80
    permission: projects.view
  view:
    title: Search
    workspace: projects
    route: /workspace/projects/search
    permission: projects.view

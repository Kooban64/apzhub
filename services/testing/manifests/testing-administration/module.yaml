manifestSchemaVersion: "1.0"
id: testing-administration
name: Administration
version: 0.2.0
kind: module

metadata:
  category: quality
  description: APZ TCMS Testing — Administration settings (APZTCMS-010).

dependencies:
  platform: []
  services:
    - testing-service

module:
  category: quality
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: testing
    parent: testing
    label: Administration
    icon: settings
    route: /workspace/testing/administration
    order: 150
    permission: testing.admin
  view:
    title: Administration
    workspace: testing
    route: /workspace/testing/administration
    permission: testing.admin

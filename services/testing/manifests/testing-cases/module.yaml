manifestSchemaVersion: "1.0"
id: testing-cases
name: Cases
version: 0.2.0
kind: module

metadata:
  category: quality
  description: APZ TCMS Testing — Test cases and steps (APZTCMS-010).

dependencies:
  platform: []
  services:
    - testing-service

module:
  category: quality
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: testing
    parent: testing
    label: Cases
    icon: file-check
    route: /workspace/testing/cases
    order: 50
    permission: testing.cases.read
  view:
    title: Cases
    workspace: testing
    route: /workspace/testing/cases
    permission: testing.cases.read

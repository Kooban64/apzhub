manifestSchemaVersion: "1.0"
id: testing-evidence
name: Evidence
version: 0.2.0
kind: module

metadata:
  category: quality
  description: APZ TCMS Testing — Evidence metadata (APZTCMS-010).

dependencies:
  platform: []
  services:
    - testing-service

module:
  category: quality
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: testing
    parent: testing
    label: Evidence
    icon: paperclip
    route: /workspace/testing/evidence
    order: 80
    permission: evidence.read
  view:
    title: Evidence
    workspace: testing
    route: /workspace/testing/evidence
    permission: evidence.read

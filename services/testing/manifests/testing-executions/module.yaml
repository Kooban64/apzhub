manifestSchemaVersion: "1.0"
id: testing-executions
name: Manual Execution
version: 0.2.0
kind: module

metadata:
  category: quality
  description: APZ TCMS Testing — Manual execution and runs (APZTCMS-010).

dependencies:
  platform: []
  services:
    - testing-service

module:
  category: quality
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: testing
    parent: testing
    label: Manual Execution
    icon: play
    route: /workspace/testing/executions
    order: 60
    permission: testing.executions.read
  view:
    title: Manual Execution
    workspace: testing
    route: /workspace/testing/executions
    permission: testing.executions.read

manifestSchemaVersion: "1.0"
id: testing-requirements
name: Requirements
version: 0.2.0
kind: module

metadata:
  category: quality
  description: APZ TCMS Testing — Requirements and risks (APZTCMS-010).

dependencies:
  platform: []
  services:
    - testing-service

module:
  category: quality
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: testing
    parent: testing
    label: Requirements
    icon: list-checks
    route: /workspace/testing/requirements
    order: 20
    permission: testing.requirements.read
  view:
    title: Requirements
    workspace: testing
    route: /workspace/testing/requirements
    permission: testing.requirements.read

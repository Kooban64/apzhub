manifestSchemaVersion: "1.0"
id: testing-suites
name: Suites
version: 0.2.0
kind: module

metadata:
  category: quality
  description: APZ TCMS Testing — Test suites (APZTCMS-010).

dependencies:
  platform: []
  services:
    - testing-service

module:
  category: quality
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: testing
    parent: testing
    label: Suites
    icon: folders
    route: /workspace/testing/suites
    order: 40
    permission: testing.suites.read
  view:
    title: Suites
    workspace: testing
    route: /workspace/testing/suites
    permission: testing.suites.read

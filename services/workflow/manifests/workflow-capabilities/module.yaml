manifestSchemaVersion: "1.0"
id: workflow-capabilities
name: Capabilities
version: 0.1.0
kind: module

metadata:
  category: workflow
  description: Workflow Workbench — Capabilities view (APZHUB-PLATFORM-WORKFLOW-006).

dependencies:
  platform: []
  services:
    - workflow-service

module:
  category: workflow
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: workflow
    parent: workflow
    label: Capabilities
    icon: puzzle
    route: /workspace/workflow/capabilities
    order: 100
    permission: workflow.engine.capabilities
  view:
    title: Capabilities
    workspace: workflow
    route: /workspace/workflow/capabilities
    permission: workflow.engine.capabilities

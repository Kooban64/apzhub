manifestSchemaVersion: "1.0"
id: workflow-definitions
name: Processes
version: 0.1.0
kind: module

metadata:
  category: workflow
  description: Workflow Workbench — Definitions view (APZHUB-PLATFORM-WORKFLOW-006).

dependencies:
  platform: []
  services:
    - workflow-service

module:
  category: workflow
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: workflow
    parent: workflow
    label: Processes
    icon: file-code
    route: /workspace/workflow/definitions
    order: 10
    permission: workflow.view
  view:
    title: Processes
    workspace: workflow
    route: /workspace/workflow/definitions
    permission: workflow.view

manifestSchemaVersion: "1.0"
id: workflow-diagnostics
name: Diagnostics
version: 0.1.0
kind: module

metadata:
  category: workflow
  description: Workflow Workbench — Diagnostics view (APZHUB-PLATFORM-WORKFLOW-006).

dependencies:
  platform: []
  services:
    - workflow-service

module:
  category: workflow
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: workflow
    parent: workflow
    label: Diagnostics
    icon: stethoscope
    route: /workspace/workflow/diagnostics
    order: 90
    permission: workflow.view
  view:
    title: Diagnostics
    workspace: workflow
    route: /workspace/workflow/diagnostics
    permission: workflow.view

manifestSchemaVersion: "1.0"
id: workflow-notifications
name: Notifications
version: 0.1.0
kind: module

metadata:
  category: workflow
  description: Workflow Workbench — Notifications view (APZHUB-PLATFORM-WORKFLOW-006).

dependencies:
  platform: []
  services:
    - workflow-service

module:
  category: workflow
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: workflow
    parent: workflow
    label: Notifications
    icon: bell
    route: /workspace/workflow/notifications
    order: 60
    permission: workflow.admin
  view:
    title: Notifications
    workspace: workflow
    route: /workspace/workflow/notifications
    permission: workflow.admin

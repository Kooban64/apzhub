manifestSchemaVersion: "1.0"
id: workflow-search
name: Search
version: 0.1.0
kind: module

metadata:
  category: workflow
  description: Workflow Workbench — Search view (APZHUB-PLATFORM-WORKFLOW-006).

dependencies:
  platform: []
  services:
    - workflow-service

module:
  category: workflow
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: workflow
    parent: workflow
    label: Search
    icon: search
    route: /workspace/workflow/search
    order: 70
    permission: workflow.view
  view:
    title: Search
    workspace: workflow
    route: /workspace/workflow/search
    permission: workflow.view

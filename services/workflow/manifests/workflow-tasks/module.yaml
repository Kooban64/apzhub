manifestSchemaVersion: "1.0"
id: workflow-tasks
name: Participants
version: 0.1.0
kind: module

metadata:
  category: workflow
  description: Workflow Workbench — Tasks view (APZHUB-PLATFORM-WORKFLOW-006).

dependencies:
  platform: []
  services:
    - workflow-service

module:
  category: workflow
  status: enabled

workbench:
  navigation:
    level: sidebar
    workspace: workflow
    parent: workflow
    label: Participants
    icon: list-todo
    route: /workspace/workflow/tasks
    order: 40
    permission: workflow.tasks.view
  view:
    title: Participants
    workspace: workflow
    route: /workspace/workflow/tasks
    permission: workflow.tasks.view
